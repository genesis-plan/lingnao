# 灵境 LingJing × 外部论文 消费对照（v3.0）

> 日期：2026-08-28
> 目的：吸收外部前沿思想完善「世界通用大脑」，并诚实记录哪些**已落地到代码（真消费）**、哪些**仅是路线（待做）**。
> 原则：不虚构、不夸大。凡标「路线」的项，内核当前未实现对应算法，仅作为设计对齐或后续计划。

## 一、总览表

| # | 论文 / 思想 | 对应内核模块 | 实装状态 | 自测证据 |
|---|---|---|---|---|
| 1 | Pearl 前门准则（do-calculus） | `causalEffect` 前门分支 | ✅ **真消费** | `causal-effect-frontdoor`（ACE=0.25 还原真值） |
| 2 | Wienöbst 自动识别引擎（causal-im） | `causalEffect` 准则选择 | ⚠️ **路线** | —（仅手调解 `mediator`/`adjustSet`） |
| 3 | CoVe 链式验证（自验证反幻觉） | `generateAudit.selfVerification` | ✅ **真消费** | `self-verify`（passed=true） |
| 4 | Reflexion 语言反思闭环 | `generateAudit.reflection` + 第四层 | ✅ **真消费** | `self-verify`（reflection=optimal） |
| 5 | Tree-of-Thoughts 树搜索推理 | A\* / RSG 推理状态图 | ⚠️ **对齐（非移植）** | `reason`（A\* 最优/RSG 分支回溯） |
| 6 | 神经符号 AI（神经感知+符号推理） | 整体定位 / `GROUNDING` 分层 | ✅ **定位层消费** | `grounding`（三档置信） |

## 二、逐篇对照

### ① Pearl 前门准则（do-calculus，1995/2000）
- **核心思想**：当未观测混杂 `U` 阻断所有后门路径（后门准则失效）时，若存在**完全观测的中介 `M`** 满足前门三条件（① `M` 截断所有 `X→Y` 路径；② 无 `X→M` 的后门；③ `X` 阻断所有 `M→Y` 的后门），则 do 效应仍可从纯观测数据用两段式识别：`ACE = α·β`，其中 `α` 是 `X→M` 斜率、`β` 是 `M→Y | X` 斜率。
- **对应模块**：`causalEffect(model, cause, effect, samples, {mediator})`。
- **实装（真消费）**：新增 `mediator` 分支 `front-door-adjustment-linear-SEM`：
  - 阶段1：`M ~ 1 + X`（`α`）；阶段2：`Y ~ 1 + M + X`（`β`）；返回 `ace=α·β`、`handlesUnobservedConfounder=true`。
  - 呼应已消费的 IMA 资料 `ima_304`（因果）、`ima_301`（反事实）。
  - **前门三条件由调用方声明**——内核不假定 DAG 已知（诚实边界：若中介不满足三条件，标注 `inapplicable` 而非硬算）。
- **自测证据**：合成 `X→M→Y` 且 `U→X,U→Y` 数据（后门失效），`ACE=α·β=0.5×0.5=0.25` 精确还原真值（容差 0.05）。
- **代码锚点**：`灵境.html` 第 1031–1046 行；`lingjing-mcp.js` `causal_effect` 工具已支持 `mediator` 参数。

### ② Wienöbst & Jeong 等 — do-calculus 自动识别引擎（causal-im，2024）
- **核心思想**：把「某个因果效应是否在给定图下可识别」变成一个**算法判定问题**，自动选择后门 / 前门 / 工具变量 / z-识别等准则，而非靠人手工声明。
- **对应模块**：`causalEffect` 的准则自动选择。
- **实装状态**：⚠️ **路线（部分消费）**。当前内核支持**手动指定** `opts.adjustSet`（后门集）与 `opts.mediator`（前门中介），但未实现完整的 do-calculus 识别引擎（自动判定「后门满足 / 前门满足 / 不可识别」并择优）。
- **下一步**：补 `identifiabilityEngine(model, cause, effect)`——基于已学线性 SEM 的邻接关系，自动枚举后门集、检测中介前门三条件，返回 `{method, applicable, reason}`。属明确待做项，不虚构当前已具备。

### ③ CoVe — Chain-of-Verification（Dhuliwala et al., Google, 2023, EMNLP）
- **核心思想**：生成答案后，用一组**验证性问题**对答案做「反向证伪」（plan → 提问 → 独立回答 → 校验），显著降低幻觉。
- **对应模块**：七段审计新增 **自验证段（selfVerification）**。
- **实装（真消费）**：`generateAudit` 在七段之外增 `selfVerification` 段——对结论做反向证伪尝试：
  - 检查霍尔证明链自洽、硬/软约束是否满足、证据是否真能支撑结论、是否存在已知未观测混杂警示；
  - 返回 `{ checked:true, passed:boolean, challenges:[...] }`。`passed=false` 时列出具体证伪点。
- **自测证据**：`self-verify` 项断言 `aud.selfVerification.checked===true && typeof aud.selfVerification.passed==='boolean'`（在可判定最优路径上 `passed=true`）。
- **代码锚点**：`灵境.html` `generateAudit` 内 `selfVerification`；`lingjing-mcp.js` 第 848–853 行自测。

### ④ Reflexion — Verbal Reinforcement Learning（Shinn et al., 2023, NeurIPS）
- **核心思想**：智能体把失败经验写成**语言反思**存入记忆，下一轮据此避免重犯（verbal reinforcement，不更新权重）。
- **对应模块**：七段审计 **反思段（reflection）** + 第四层「反思与演化中枢」`SelfLearn`。
- **实装（真消费）**：`generateAudit` 增 `reflection` 段——元认知复盘，返回 `{ verbalReinforcement:true, insight:string, status }`（`status` 如 `optimal`/`needs-review`）；与 `SelfLearn` 的正/负/边界样本闭环、`slMonitor` 降级互相呼应。
- **自测证据**：`self-verify` 项断言 `aud.reflection.verbalReinforcement===true && aud.reflection.insight.length>0`。
- **代码锚点**：`灵境.html` `generateAudit` 内 `reflection`；第四层 `SelfLearn` 模块。

### ⑤ Tree-of-Thoughts — 树搜索式推理（Yao et al., 2023, NeurIPS）
- **核心思想**：把推理建模为**树搜索**，多分支展开 + 状态评估 + 回溯，超越线性思维链（CoT）。
- **对应模块**：系统2 推理 `reason` 的 A\* + **RSG 推理状态图**（记录分支/回溯/剪枝）。
- **实装状态**：⚠️ **对齐（非移植）**。内核的 A\* + RSG 与 ToT 同宗——都是「搜索替代线性生成」，且**有成本最优保证 + 可审计**（ToT 的 LLM 自评多候选是概率采样，与灵境「不幻觉」定位冲突，故**不移植采样式扩展**）。诚实标注：灵境用确定性图搜索而非 LLM 采样的树扩展。
- **可借鉴的后续路线**：在**系统1 快答**层引入基于经验库置信度的多候选生成（确定性、可审计），而非 LLM 自由采样。

### ⑥ 神经符号 AI（d'Avila Garcez / Gori 等 — 概率逻辑编程、DeepProbLog）
- **核心思想**：神经（感知/学习）与符号（推理/约束）结合；符号层保证可解释、可验证；概率逻辑把不确定性纳入逻辑。
- **对应模块**：**产品整体定位** + `GROUNDING` 三档置信分层。
- **实装（定位层消费）**：灵境刻意把免费 LLM（神经）限制在**感知/解释**两个前端，推理、审计、因果全部在**本地确定性符号内核**（A\* / 知识库 / 霍尔证明 / do-演算）。`GROUNDING` 把「PERCEPTION 可能幻觉 / KERNEL 确定性 / PROOF 可验证」做成返回值机读字段，正是神经符号边界的显式化。
- **代码锚点**：`GROUNDING` / `groundingMeta` / `askBrain.explanation.disclaimer`。

## 三、诚实边界汇总
- **真消费（已落代码、可被自测验证）**：①前门准则 ③CoVe 自验证 ④Reflexion 反思。
- **定位层消费**：⑥神经符号边界（体现在 `GROUNDING` 与 `ARCHITECTURE.md`）。
- **对齐但非移植**：⑤ToT（用确定性 A\*/RSG 替代概率采样树扩展）。
- **路线（待做，不虚构）**：②do-calculus 自动识别引擎（补 `identifiabilityEngine`）。
- 所有 `lite` 标注（Z3-lite / PC-lite / SimHash LSH / 线性 SEM）维持不变：手写轻量等价实现，非工业级外部求解器，均确定性、可审计、不虚构。

## 四、下一步论文路线（由用户拍板）
1. **②补全 do-calculus 自动识别引擎**：`identifiabilityEngine()` 自动选后门/前门/不可识别。
2. **贝叶斯因果推断**：在线性 SEM 上引入后验分布（呼应 PC-lite 升级），给出 ACE 的置信区间而非单点。
3. **神经符号检索增强**：把 IMA 数学库（431 条）接成符号规则库，做「符号推理 + 库检索」双通道。
4. **联邦/边缘部署**：把内核下沉到物理载体（与第五层元认知 + 边缘联邦呼应），需参考联邦学习前沿。

## 五、自测证据（本轮新增）
```
SELFTEST OK — 全部 45 项工具验证通过：
  ✓ causal-effect-frontdoor :: ace=0.25 α=0.5 β=0.5   （前门准则真消费）
  ✓ self-verify :: passed=true reflection=optimal      （CoVe + Reflexion 真消费）
  ✓ grounding :: percept.tier=UNVERIFIED_LLM reason.tier=DETERMINISTIC
  …（共 45 项，含 36 MCP 工具 + 内核模块）
```
