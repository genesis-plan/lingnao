# 灵境 LingJing × 外部通用大脑 / 认知架构 消费对照（v3.0）

> 日期：2026-08-28
> 目的：调研世界上真实存在的「通用大脑 / 认知架构」构想（LeCun AMI、Active Inference、NARS、SOAR/ACT-R、OpenCog、全局工作空间、Generative Agents/Voyager、神经符号 AI），逐一定位它们能**消费进灵境内核**的部分，并诚实标注消费状态。
> 原则：**不虚构、不夸大**。三档标注——
> - ✅ **真消费**：思想已落到代码、可被自测验证；
> - 🔶 **可消费 / 已架构对应**：设计已对齐、部分落代码，但核心算法仍待补；
> - ⚠️ **仅路线**：内核当前未实现对应算法，仅作为后续计划（诚实不标成已有）。
> 注：与 `灵境_论文消费对照.md` 互补——本文档看「架构级构想」，论文文档看「具体论文算法」。重叠处（神经符号、CoVe/Reflexion）交叉引用，不重复编造。

## 一、总览表（8 个外部构想 → 灵境模块）

| # | 外部构想 | 最可消费的内核模块 | 消费状态 |
|---|---|---|---|
| ① | LeCun AMI / JEPA（世界模型） | `setWorld` + `perceiveBelief` + `learnWorldModel` / `counterfactual` + 系统2 `reason` | ✅ 真消费（显式世界模型+信念更新+反事实）；⚠️ 潜空间自监督表征=路线 |
| ② | Active Inference / FEP（自由能原理） | `perceiveBelief`（变分自由能最小化）+ `metaCognition` 探索-利用 | ✅ 思想消费（贝叶斯信念更新）；⚠️ EFE 显式分解=路线 |
| ③ | NARS（非公理推理，王培） | `KB` 置信度(频率,置信) + `GROUNDING` + `SelfLearn` | ✅ 真消费（频率/置信真值思想）；⚠️ NAL 逻辑=路线 |
| ④ | SOAR / ACT-R（块化/子目标） | `KB.distillRules`（FP-Growth 规则蒸馏）+ 子目标硬约束 | ✅ 真消费（chunking 思想）；⚠️ 产生式解释器=路线 |
| ⑤ | OpenCog / PLN（超图知识+概率逻辑） | `KB`/`KB.cogGraph` + `metaKnowledgeRouter`（注意力经济） | 🔶 已架构对应（AtomSpace/ECAN 思想）；⚠️ PLN/MeTTa=路线 |
| ⑥ | 全局工作空间理论 GWT / LIDA | `EventBus`(EDA) + `KBFabric`(Data Fabric) + 第五层元认知 | ✅ 真消费（黑板/事件总线/广播）；🔶 意识协调=已对应 |
| ⑦ | Generative Agents / Voyager | `SelfLearn` + `KB.distillRules`/`KB.ann` + `generateAudit.selfVerification` | ✅ 真消费（技能库+自验证+终身学）；🔶 自动课程=已对应 |
| ⑧ | 神经符号 AI（双系统 System1+2） | 整体定位 + `GROUNDING` 三档 + 系统1/系统2 | ✅ 真消费（定位+双系统+边界字段） |

## 二、逐构想对照

### ① LeCun AMI / JEPA — 世界模型驱动的自主智能
- **核心思想**（AMI 2022 / V-JEPA2 2025 / AMI Labs $1B 2025）：智能 = 世界模型 + 代价/价值 + 执行器；世界模型用**自监督潜空间预测**（JEPA：能量模型，预测表征而非像素）学会「行动→状态变化」；V-JEPA2 在真实机器人上零样本规划。关键：**不生成像素，预测抽象表征**，从而样本高效、可规划。
- **对应模块**：灵境的「世界图」`setWorld` + 信念更新 `perceiveBelief` + 世界模型学习 `learnWorldModel`/`simulate`/`counterfactual` + 系统2 `reason`（规划器）。
- **实装（真消费）**：
  - `setWorld` 把世界显式定义为**状态图 + 边概率 P + 不确定性**，等价于「被外部给定的世界模型」（确定性版，而非潜空间）；
  - `perceiveBelief(initial, observations, transitionKernel, maxIter, tol)` 用**贝叶斯滤波迭代 + Banach 压缩映射收敛检测**做信念状态估计——正是世界模型的「状态估计」环节（确定性等价，无随机潜空间）；
  - `learnWorldModel(samples)` + `counterfactual` 用**线性 SEM + Pearl 反事实三步法（abduction→action→prediction）**学结构方程并做反事实——直接对应 AMI 的「世界模型可反事实推演」；
  - 系统2 `reason`（A\* + RSG）即 AMI 的「critic/规划器」：在世界模型里搜代价最优路径。
- **诚实边界（路线）**：JEPA 式**潜空间自监督表征学习**（V-JEPA2 的编码器/能量模型）灵境未做——灵境用**显式图**而非学习到的潜空间，这是有意为之（可审计、不幻觉），故标注「路线不移植」；AMI 的在线 actor/configurator 闭环**执行器**当前为离线规划（无真执行器），属路线。
- **交叉**：与 `灵境_论文消费对照.md` ⑤ToT、⑥神经符号互补。

### ② Active Inference / 自由能原理（FEP）
- **核心思想**（Friston）：生物/智能体通过**最小化变分自由能（ surprisal）**维持稳态；感知最小化 FE（更新信念），行动最小化 FE（改变世界）；**期望自由能 EFE** 分解为**信息价值（epistemic / 好厅，探索未知）** + **语用价值（pragmatic，达成目标）**；**马尔可夫毯**划内/外边界。
- **对应模块**：`perceiveBelief`（感知降 FE）+ `metaCognition`（探索-利用决策）+ 不确定性量化 `quantifyUncertainty` / 一致性 C(K) + 物理载体接入（毯边界）。
- **实装（思想消费）**：
  - `perceiveBelief` 的贝叶斯滤波迭代 = **感知阶段最小化自由能**（变分推断的确定性等价），已在内核实装；
  - `metaCognition` 的 `exploreExploit`：`gaps.length>0 || globalUncertainty>0.5 → explore`，正是 EFE 中**信息价值（探索）vs 语用价值（利用）**的离散化决策——**架构已对应**；
  - 不确定性 `1-C(K)`、`quantifyUncertainty(path)` = surprisal 的代理指标。
- **诚实边界（仅路线）**：未显式计算 **EFE = 信息项 + 语用项** 的连续分解公式；未实现**主动推理（active inference）的在线行动选择**（当前规划为离线给定）。马尔可夫毯仅概念对齐到「物理载体硬软约束 / <20% 禁离充电座」。

### ③ NARS — 非公理推理系统（王培）
- **核心思想**（Wang Pei, OpenNARS/ONA）：**AIKR 假设**（知识不充分、资源不充分）；真值 = **(frequency, confidence) 二元组**而非绝对真假；从经验中按需学习；NAL 逻辑；**非公理**——不预设完备公理集。
- **对应模块**：`KB` 样本+置信度 + `GROUNDING` + `SelfLearn`（经验闭环）+ `metaCognition` 一致性。
- **实装（真消费）**：
  - `KB` 正·负·边界样本 + **confidence** + 冲突检测 = NARS **(frequency, confidence)** 真值思想的直接落地：边界样本=低 frequency、负样本=反例、confidence=置信度；
  - `GROUNDING` 三档（PERCEPTION 可能幻觉 / KERNEL 确定性 / PROOF 可验证）= **非公理**立场的机读化——不假设 LLM 感知绝对真，按证据给置信；
  - `SelfLearn` 正/负/边界样本闭环 = **经验驱动、知识不充分假设下的学习**（AIKR 的实践版）；
  - `metaCognition` 一致性 `C(K)=1-冲突率-α·H(K)` = 冲突率越高 confidence 越低，呼应 NARS 置信衰减。
- **诚实边界（仅路线）**：**NAL 逻辑规则集**未移植——灵境用图搜索(A\*)+符号求解(Z3-lite)+因果(do-演算)替代 NAL，不声称实现 NARS 推理机；OpenNARS/ONA 的时序/过程推理未接。

### ④ SOAR / ACT-R — 块化与通用子目标
- **核心思想**：**chunking**（把一次问题求解中的「状态-操作」对固化为产生式，加速后续同类问题）；**universal subgoaling**（自动生成子目标）；问题空间（状态+操作）；陈述性/程序性记忆；产生式系统。
- **对应模块**：`KB.distillRules`（规则蒸馏）+ `reason` 子目标硬约束 + `system2` 状态空间。
- **实装（真消费）**：
  - `KB.distillRules(minSupport)` 用 **FP-Growth** 从经验样本蒸馏频繁规则 = **chunking 思想**（经验→可复用规则）；
  - `reason(start, goal, {hard, soft})` 的硬约束 + RSG 分支 = **universal subgoaling**（把「避开 B」固化为子目标）；
  - `system1`（KB 高置信复用）≈ 程序性记忆的快通路；`KB` 陈述样本 ≈ 陈述性记忆。
- **诚实边界（仅路线）**：未实现 SOAR 式**产生式解释器**（production rule interpreter）/ 实时 chunking 在线的再编译；问题空间为显式图而非 Soar 的算子空间。

### ⑤ OpenCog / PLN — 超图知识 + 概率逻辑 + 注意力经济
- **核心思想**（Goertzel）：**AtomSpace** 超图知识库；**PLN** 概率逻辑网；**ECAN** 注意力经济（稀缺注意力按重要性分配）；**Hyperon/MeTTa** 新一代元语言；**认知协同**（多子系统互补）。
- **对应模块**：`KB`/`KB.cogGraph`（认知图谱）+ `metaKnowledgeRouter`（注意力路由）+ 第五层元认知协调。
- **实装（已架构对应）**：
  - `KB` 边+概率+置信 的图式知识 = **AtomSpace 思想**（超图知识库的确定性版）；`KB.cogGraph()` 输出认知拓扑；
  - `metaKnowledgeRouter(gaps)` 按**知识缺口路由**到对应知识源 = **ECAN 注意力经济**（稀缺推理资源按重要性分配）的简化版；
  - 第五层 `metaCognition` 监控+冲突仲裁 = **认知协同**的协调层。
- **诚实边界（仅路线）**：**PLN 概率逻辑推理**未移植（灵境用贝叶斯+因果+符号求解替代）；**Hyperon/MeTTa** 元语言未用；AtomSpace 的attention 值为简化标量而非 ECAN 的 Hebbian/经济模型。

### ⑥ 全局工作空间理论 GWT / LIDA
- **核心思想**（Baars / Franklin）：**全局工作空间 = 黑板**，多专用模块竞争写入；胜出信息被**全局广播**（即「意识」）；LIDA = GWT + 注意 + 转译。模块并行、仅广播内容被全局共享。
- **对应模块**：`EventBus`(EDA 事件总线) + `KBFabric`(Data Fabric 版本化) + 第五层元认知协调。
- **实装（真消费）**：
  - `EventBus`（发布/订阅事件总线）= **黑板 / 全局工作空间**的通信骨架——各模块（感知/推理/审计/元认知）经总线协同；
  - `KBFabric`（版本化知识 Data Fabric）= **被广播内容的持久化与溯源**（versioned shared memory）；
  - 第五层 `metaCognition` 的监控+仲裁 = **全局广播的协调者**（决定哪些结论进入可审计输出）。
- **实装（已对应）**：MCP **36 工具面**即「全局广播出口」——内核能力经统一面暴露给外部智能体。
- **诚实边界（路线）**：未实现 LIDA 式**意识循环**（codelet 竞争 + 转译）的实时调度；广播为事件驱动而非「竞争胜出」模型。

### ⑦ Generative Agents / Voyager — 自动课程 + 技能库 + 自验证
- **核心思想**（Park et al. 2023 / Voyager, Minecraft 2023）：智能体**自建任务课程**（auto-curriculum）、维护**可检索技能库**（向量库）、用**迭代提示 + 自我验证**改进、在开放世界**终身学习**；Voyager = 技能库 + 自动课程 + 迭代提示三件套。
- **对应模块**：`SelfLearn`（终身学）+ `KB.distillRules`/`KB.ann`（技能/规则库+检索）+ `generateAudit.selfVerification`（自验证）。
- **实装（真消费）**：
  - `KB.distillRules`（规则蒸馏）+ `KB.ann`（SimHash LSH 近似检索）+ `KB.cogGraph` = **技能库**（确定性、可审计版，替代向量库）；
  - `generateAudit.selfVerification`（**CoVe 链式证伪**，见论文对照③）= **自我验证**环节，已落代码；
  - `SelfLearn`（正/负/边界样本闭环）+ `learnWorldModel` = **终身学习**（经验累积、不遗忘已验证知识）；
  - `metaCognition` 探索-利用 = **自动课程**的离散版（自动决定「该学什么」）。
- **诚实边界（已对应但未显式）**：未实现 Voyager 式**智能体自提自然语言任务**（auto-curriculum 的「自生成目标」），当前课程由外部 `setWorld`/用户给定；技能库为规则而非代码脚本。

### ⑧ 神经符号 AI（双系统 System1+System2）— IJCAI 2025 综述定位
- **核心思想**：三种范式——Symbolic→LLM（符号给 LLM 接地）、LLM→Symbolic（LLM 抽取符号）、Symbolic+LLM（混合）；本质是**双系统**：System1 快（神经/直觉）、System2 慢（符号/推理）。
- **对应模块**：**产品整体定位** + `GROUNDING` 三档 + 系统1/系统2 分流。
- **实装（真消费，定位层）**：
  - 免费 LLM（神经，`perceiveLLM`/`explainWithLLM`/`askBrain`）被**严格限制在感知与解释前段**；推理/审计/因果全在**本地确定性符号内核**（A\*/KB/霍尔证明/do-演算）= 标准神经符号边界；
  - 系统1（KB 高置信快答）+ 系统2（A\*/RSG/符号/Z3-lite/霍尔）= **双系统**架构实装；
  - `GROUNDING` 把「PERCEPTION 可能幻觉 / KERNEL 确定性 / PROOF 可验证」做成返回值机读字段 = 神经-符号边界的**显式化**（详见论文对照⑥）。
- **诚实边界**：不移植「LLM→Symbolic 自动抽取世界模型」的概率路径（与「不幻觉」冲突）；系统1 真 kNN-决策树分流仍待补（当前为置信阈值分流）。

## 三、诚实边界汇总（不虚构）

| 构想 | 真消费（落代码、可自测） | 已架构对应 | 仅路线（待做，不虚构） |
|---|---|---|---|
| ① LeCun AMI/JEPA | 显式世界模型 `setWorld`、信念更新 `perceiveBelief`、反事实 `counterfactual`、规划 `reason` | — | 潜空间自监督表征(V-JEPA2)、在线 actor 闭环 |
| ② Active Inference | 贝叶斯信念更新 `perceiveBelief` | 探索-利用决策 `metaCognition` | EFE 显式分解、主动推理在线行动选择 |
| ③ NARS | 频率/置信真值 `KB.confidence`、非公理 `GROUNDING`、经验学习 `SelfLearn` | 一致性衰减 `metaCognition` | NAL 推理机、OpenNARS 时序/过程 |
| ④ SOAR/ACT-R | 规则蒸馏 `KB.distillRules`（chunking） | 子目标硬约束 `reason`、双记忆分流 | 产生式解释器、在线 chunking 再编译 |
| ⑤ OpenCog/PLN | — | AtomSpace(`KB`/`cogGraph`)、ECAN(`metaKnowledgeRouter`) | PLN 概率逻辑、Hyperon/MeTTa |
| ⑥ GWT/LIDA | 黑板/总线 `EventBus`、持久化 `KBFabric`、广播出口 MCP 36 | 协调 `metaCognition` | LIDA 意识循环(codelet 竞争) |
| ⑦ GenAgents/Voyager | 技能库 `KB.*`+`ann`、自验证 `selfVerification`、终身学 `SelfLearn` | 自动课程 `metaCognition` | Voyager 自提任务(auto-curriculum) |
| ⑧ 神经符号 | 定位+双系统+边界 `GROUNDING` | — | LLM→Symbolic 自动抽取、系统1 kNN 分流 |

> 所有 `lite` 标注（Z3-lite / PC-lite / SimHash LSH / 线性 SEM）维持不变：手写轻量等价实现，非工业级外部求解器，均确定性、可审计、不虚构。
> 与论文消费对照的重叠项（⑥神经符号、③CoVe、④Reflexion）以论文文档为准，本文档从「架构级」角度补充定位。

## 四、消费结论（给用户）

1. **已真消费的思想（可直接对外讲）**：世界模型显式化、贝叶斯信念更新、频率/置信真值、chunking 规则蒸馏、AtomSpace/ECAN 注意力、全局工作空间总线、技能库+自我验证+终身学习、神经符号双系统边界。这些不是「套概念」，都落了代码、可被 `selftest`（45/45）和 `demo-common.js`（6/6）复跑验证。
2. **最值得继续消费（路线→待做）**，按性价比排序：
   - **A. EFE 显式分解**（②）：把 `metaCognition` 的探索-利用从离散开关升级为「信息价值+语用价值」连续权衡——改动小、叙事强（"会好奇的大脑"）。
   - **B. PLN/概率逻辑替代或补充**（⑤）：在 `KB` 上做概率推理，强化不确定性传播。
   - **C. Voyager 自提任务**（⑦）：让内核自己生成学习目标（auto-curriculum），是"通用大脑"最该有的能力。
   - **D. 在线 actor 闭环**（①）：从离线规划走向真执行器（呼应第五层边缘联邦）。
3. **不消费 / 不移植（有意为之）**：JEPA 潜空间自监督（不可审计）、LLM→Symbolic 自动抽取世界模型（与"不幻觉"冲突）、ToT 概率采样树扩展（不可审计）。这些在对外口径中讲成"主动取舍"而非"短板"。

## 五、下一步路线（由用户拍板，AI 不替定）

- 路线 A（EFE 探索-利用连续化）— 小改、强叙事，推荐首选。
- 路线 B（PLN 概率推理补充 `KB`）。
- 路线 C（auto-curriculum 自提任务）。
- 路线 D（在线执行器 + 边缘联邦，呼应第五层）。
- 以上任一落地后，同步更新 `灵境_完整骨架对照.md` 与自测，并推仓（NPM publish 仍先不推，除非用户明确）。
