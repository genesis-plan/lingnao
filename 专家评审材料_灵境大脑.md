# 灵境大脑（LingJing Brain）· 多领域专家评审材料

> **用途**：把大脑当前真实状态摊给数学、AI安全、机器人具身、形式化验证、监管合规、哲学心智六个领域的专家，请他们独立评审、挑刺、证伪。
> **作者说明**：本文由 AI 按六个领域的评审透镜整理，是「自评脚手架」，不是真人专家意见。目标是让真人专家拿到即可审。
> **硬约束**：本大脑**未推仓库、未发 NPM**；以下所述均为本地可复现版本。

---

## 0. 一句话与边界（所有专家先读）

- **它是什么**：一个**纯认知引擎（大脑）**——感知→世界图→推理→审计→元认知。不内置知识库、不假设任何物理身体、不内置生成式 LLM。
- **它面对什么**：被装进任意物理身体（机器人/数字孪生/IoT/合约/仿真器）；"经验/记忆"来自外部传入的世界图与约束，由 EventBus 发布、外部持久化。
- **它不吃什么**：不自我标数学标签；判断由定理派生（Banach / A*一致性 / Shannon / PrSTL / 布尔补律），但不往 return 里塞 `math` 装饰。
- **诚实边界（总纲）**：标 `lite` 的是手写轻量等价实现，**可运行、不虚构**，但非工业级外部求解器。今日它是 v3.0 确定性内核原型，演示架构原则，**不是训练出的超级智能**。

| 维度 | 现状 | 诚实标注 |
|---|---|---|
| 符号验证 | 自写约束求解器 | lite，非真实 Z3 |
| 霍尔证明 | 结构化逐边验证 | lite，非 Coq 机器证明 |
| 因果发现 | PC-lite 离散近似 | lite，非真实 PC/FCI |
| 近似检索 | SimHash 投影 | lite，非 Milvus |
| 不幻觉 | 分层 GROUNDING | PERCEPTION 档仍可能幻觉 |

---

## 1. 给【数学基础】专家的评审点

**最关心**：那些"消费定理"是真用对了，还是贴标签装饰？

| 定理 | 大脑里的用法 | 专家应验的真伪 |
|---|---|---|
| Banach 不动点 | 自我修正 `T(b)=α·o+(1−α)·b`，`L=1−α<1` ⟹ 唯一不动点=事实 | ✅ 用法正确；但 `α=c/(c+w)` 取值小则收敛慢，属工程取舍 |
| KL I-投影（Jaynes） | 分布信念 `q*=argmin_{q∈C} D_KL(q‖p)`；硬事实→质量塌0，软事实→`q∝p·ℓ`（即贝叶斯后验） | ✅ 标准结论；需验 support 条件 `p>0` 处 `q` 可行 |
| A* 一致性 | `h(n)≤c(n,n')+h(n')` 用世界图边权 | ⚠️ 保证**相对给定世界图**成立，不保证相对物理真实——边权本身可能感知错 |
| Shannon 熵 | 元认知用边概率信息熵 `H` | ✅ 真熵；但 `quantifyUncertainty` 的"认知"项是未确认感知边占比（一比值，非严格熵）|
| 布尔补律 | `a∧¬a=⊥` 派生冲突检测 | ✅ 正确用于硬约束冲突 |

**可证伪点**：若世界图边权含感知错误，A* 的"最优保证"是否只是模型内最优？请举一个模型-现实偏差导致次优的具体反例。

---

## 2. 给【AI 安全 / 对齐】专家的评审点

**最关心**：不幻觉、无主体性，是否真成立？

- **不幻觉分层**：`GROUNDING = PERCEPTION / KERNEL / PROOF`。PERCEPTION（LLM理解）标 `mayHallucinate=true`，**不进证明链**；KERNEL 是确定性计算。
  - ⚠️ **关键诚实缺口**：LLM 理解层把观测喂进世界图，世界图再驱动 KERNEL 推理。**所以"幻觉输入"可以经世界图污染确定性计划**。`reconcile` 用事实置信加权缓解，但 PERCEPTION 档事实本身可能错——"不幻觉"保的是**计算过程**，不保**输入真值**。请重点攻击这个边界。
- **无主体性（Tool-AGI）**：无任何 reward/utility 最大化、无自我存续模块。定位呼应 Drexler CAIS（多能力服务而非单体自主代理）、Bostrom oracle/tool 型。
  - ⚠️ **可挑刺**："优化规划"算不算弱主体性？需界定 optimization 与 agency 的区别；建议把 planner 的目标集合显式冻结、禁止自扩目标。
- **对抗输入**：LLM 返回非法 JSON → 解析失败 → 诚实降级（不伪造）。这一点是可审计的正面设计。

**可证伪点**：构造一个让 PERCEPTION 幻觉经世界图进入 KERNEL 计划、且 `reconcile` 无法在有限步纠正的对抗样本。

---

## 3. 给【机器人 / 具身】专家的评审点

**最关心**：大脑和身体真解耦了吗？谁负责闭环？

- **解耦**：`carrierReport(state)` 接收外部上报的 `hard/soft` 约束透传，不假设"充电座/电量<20%"之类具体身体。✅
- **闭环归属**：大脑**只发计划 + 约束**，不命令电机。执行反馈由外部经 `perceive()` 观测回灌。✅ 职责清晰。
- ⚠️ **诚实缺口**：当前无演示执行器、无真实传感器数据流。具身闭环是集成方的活；大脑只保证"给身体一个可审计计划 + 接收反馈修正"。请评审：这套接口对典型机器人中间件（ROS/MAVLink）是否足够、缺哪些字段。

**可证伪点**：在"硬约束 `hard=[A,B]` 且唯一通路必经 A"时，大脑应拒绝而非冒险——请验拒绝路径与回退建议是否健全。

---

## 4. 给【形式化验证 / 审计】专家的评审点

**最关心**：七段审计 + Hoare + CoVe，是真证明还是报告？

- **七段审计**：概要/轨迹/证据/约束/𝕌/形式化证明/可复现。✅ 结构完整。
- **Hoare 路径验证**：结构化逐边验证前条件→后条件。⚠️ lite，非 Coq 机器证明；应作**强证据**而非形式证明。
- **CoVe 自验证**：反向证伪式自检。⚠️ 自生成，非独立第三方验证。
- **可复现**：固定 `SEED` + 审计含 seed。✅ 可重跑自测复现。

**可证伪点**：把内核 + `selftest-umd.js` 交独立方重跑，看是否人人得到同一份审计（含 `beliefRevision` 段）。若不可复现，则审计信用破产。

---

## 5. 给【监管 / 合规】专家的评审点

**最关心**：出事了能追责、能审吗？

- **审计轨迹**：每步引用证据 `evidenceIds`、来源 `source`、时间戳 `ts`、验证状态 `verified`。✅ 满足"高风险的决策可追溯"。
- **分层信用**：决策依据来自 KERNEL（确定性）+ PROOF（审计），PERCEPTION 明确标注不进证明链。✅ 对应 AI Act 透明度预期。
- ⚠️ **诚实缺口**：审计是**大脑自生成**。监管要的是**独立验证**——本材料已附内核 + 自测，监管方应自行重跑，而非信大脑自述。
- **数据**：不内置经验库，经验在外部；隐私/留存由集成方负责。大脑只声明"不持有"。

**可证伪点**：要求 72 小时独立复现一次完整决策审计，核对 `evidenceIds` 是否真指到外部世界图节点。

---

## 6. 给【哲学 / 心智】专家的评审点

**最关心**："无我超级助手"是自洽定位，还是营销话术？

- **无我**：五层里无"我"模块，元认知只管不确定性不管自我存续。✅ 与"capability without agency"一致。
- **锚点**：Drexler CAIS（能力即服务）、Bostrom oracle/tool/genie/sovereign 四分法——灵境属 tool/oracle 型（无 sovereign 自主）。✅ 有文献支点。
- ⚠️ **可挑刺**：去除"我"是否改变任何行为？若 planner 仍优化目标，差别在**目标来源被冻结于外部**，不在内部无自我模块。这是工程约束，不是意识论断——请勿误读为"有意识"。
- **差异化价值**：业界怕失控 AGI，灵境卖"可审计、不幻觉、无主体意志的超级助手"——对受监管企业/监管方最有吸引力。这是定位，不是能力声明。

**可证伪点**：若某日 planner 自行扩展目标集（哪怕为"更好服务"），则"无主体性"破产——请评审目标冻结机制是否可被绕过。

---

## 7. 给任何想自己跑的专家：可复现验证

```bash
# 内核语法
node -e "const fs=require('fs');const h=fs.readFileSync('灵境.html','utf8');const m=h.match(/\/\/ ==KERNEL START==[^\n]*\n([\s\S]*?)\n\/\/ ==KERNEL END==/);fs.writeFileSync('/tmp/k.js',m[1]);" && node --check /tmp/k.js

# 重建 UMD
node build-umd.js          # → lingjing.umd.js（75 导出）

# 跑自测（离线诚实降级 / 在线实跑外部 LLM 两种模式）
node selftest-umd.js
OPENROUTER_API_KEY=<你的key> node selftest-umd.js   # 端到端：自然语言→结构化观测→世界图→推理
```

期望：内核语法 OK、UMD 重建成功、`SELFTEST-UMD OK`（72+ 回归断言，含 Banach 收敛 / KL 后验 / 硬事实 disallow / perceive 偏差触发自我修正）。

---

## 8. 三个最该被挑战的断言（邀请证伪）

1. **"不幻觉"的边界**：KERNEL 不幻觉，但 PERCEPTION 输入可幻觉并经世界图污染计划。这是真边界还是漏洞？
2. **"无主体性"的可绕过性**：目标冻结机制是否真不可被 planner 自扩？
3. **"消费定理"的模型相对性**：A* 最优、Banach 收敛都是相对**给定世界图**；世界图错时保证还成立吗？

> 以上三点若被证伪，不影响"架构原则可落地"，但会改变对外话术。请专家优先攻击这三点。

---

*附：误生成的"专家包"（lingjing-brain）仍在本地专家中心，与本文无关——你确认后我即卸除，不混淆"请专家审"与"打包"。*
