# 灵境 LingJing × 通用大脑 v3.0 实现对照

> 对照依据：`灵境.html` 内核（KERNEL START–END）+ `lingjing-mcp.js`（8 工具）+ 概念文档 `通用大脑v3.0架构.md`。
> 判定：✅ 已确定实装（Node 校验通过、不幻觉）｜🟡 部分实装｜⏳ TODO 桩（文档有名词无算法，诚实标注）｜❌ 未开始（v3.0 新增、连桩都无）。
> 约束：先不推 NPM / 不推仓库，先做好产品，最后一次性推。本文件本地存档，未发布。

---

## 总评

当前灵境 = v3.0 的**最小可信内核原型**：
- 已实现：世界图（外部可定义）+ A\* 单路径最优推理（硬/软约束）+ 五段审计 + 正样本知识库（置信度/冲突）+ 单链学习 + 物理载体硬软约束 + 不可判定区 𝕌。
- Node 自测 11/11 通过，确定性、可审计、不幻觉。

v3.0 概念版宣称的全部**最先进**特性（RSG 并行图搜索、系统1/2 双架构、符号-神经融合、启发式自演化、负样本空间、元知识层、元认知协调层、事件驱动 Agent 总线、Data Fabric、仓库级持续验证 / 运行时 PrSTL、边缘-云端联邦）**当前基本为零**，仅以 TODO 桩诚实标注，未虚构。

---

## 一、五层架构对照

### 第一层 物理接入层（感知与行动接口）
| 设计点 | 状态 | 说明 |
|---|---|---|
| 感知=构建认知状态图 | 🟡 部分 | `perceive()` 仅结构化 JSON 校验+指纹+合法性；无"主动构图"（Banach 信念收敛标 TODO） |
| 行动=物理锚定 | 🟡 部分 | `carrierReport()` 由电量/密度生成硬/软约束；无真"下发→执行回报→状态图增量更新"闭环 |
| 感知-行动闭环 | ⏳ 半 | learn 为手动反馈，需真实载体数据流才闭环 |

### 第二层 认知状态空间层（世界图 + 知识库）
| 设计点 | 状态 | 说明 |
|---|---|---|
| 世界图 节点/边/权重/坐标 | ✅ | `WORLD` + `setWorld` 外部可定义 |
| 世界图 概率分布/置信度/多元边(因果/时序/相似) | ❌ | 边仅权重，无概率/因果/时序；节点无动态演化 |
| 知识库·正样本经验 | ✅ | `KB` 经验表+置信度+冲突检测 |
| 知识库·负样本空间(AnyPhys) | ❌ | 仅存成功样本，无失败/失稳/临界边界库 |
| 知识库·抽象规则库(蒸馏) | ❌ | 无自动归纳 |
| 知识库·认知图谱(类比) | ❌ | 未开始 |
| 知识库·元知识层(MoEMeta/Open-MAML) | ❌ | 未开始 |
| 不可判定区 𝕌 | ✅ | `aStar` 诚实返回 `U` |

### 第三层 认知推理层（思考引擎）
| 设计点 | 状态 | 说明 |
|---|---|---|
| 系统1/系统2 双架构 | ❌ | 仅 A\*（系统2式），无毫秒级规则直觉层 |
| RSG 推理状态图(DAG 分支/合并/回溯) | ❌ | 当前 A\* 单路径最优，无分支树/回溯/并行 |
| 分布式认知搜索(KV COW / Token路由 PiMoE) | ❌ | 未开始 |
| 符号-神经融合验证器(一阶逻辑+约束求解) | ❌ | A\* 本身确定性约束搜索，无独立符号验证器双重校验 |
| 启发式自演化 | ❌ | `heuristic` 静态欧氏，`learn` 不更新启发式 |

### 第四层 反思与演化层（学习引擎）
| 设计点 | 状态 | 说明 |
|---|---|---|
| 反馈闭环(预期vs实际误差) | 🟡 部分 | `learn()` 手动反馈更新置信度，无自动预测误差计算 |
| 三重更新(置信度+转移概率+启发式) | ❌ | 仅置信度单链 |
| PAC 保证 | ⏳ TODO 桩 | 无样本复杂度界 |
| 价值对齐(Value-Aligned) | ❌ | 未开始 |
| 知识蒸馏自动化 | ❌ | 未开始 |
| 因果发现自动化(do演算) | ⏳ TODO 桩 | 未开始 |

### 第五层 元认知与协调层
| 设计点 | 状态 | 说明 |
|---|---|---|
| 全局监控/探索-利用/缺口识别/冲突仲裁/资源分配 | ❌ | **完全未实现**（v3.0 缺失最严重的一层） |

---

## 二、通信 / 数据 / 验证 / 边缘 对照

| 设计点 | 状态 | 说明 |
|---|---|---|
| EDA 事件总线(Pulsar) | ❌ | MCP 为同步 stdio，无事件流解耦 |
| 事件类型体系(8类) | ❌ | 未开始 |
| Data Fabric(联邦查询/版本/血缘/生命周期) | ❌ | 未开始 |
| 仓库级持续验证 | ❌ | 仅 `--selftest` 手动 |
| 推理时验证(证明证书) | ❌ | 未开始 |
| 运行时验证 PrSTL(安全停车) | ❌ | 未开始 |
| LLM 辅助证明合成 | ❌ | 未开始 |
| 三级部署(端/边/云) | ❌ | 未开始 |
| 联邦学习/差分隐私/拜占庭 | ❌ | 未开始 |

---

## 三、诚实边界（不虚构）

- **已确定实装（可审计、不幻觉）**：世界图 + A\* + 五段审计 + 正样本 KB + 单链学习 + 物理载体硬软约束 + 𝕌。
- **TODO 桩（文档有名词无算法，已诚实标注）**：Banach 感知、PAC 界、do 演算、世界模型/反事实、霍尔验证、分布式部署、LSH 检索。
- **纯概念未开工（v3.0 新增、连桩都无，因旧文档未含）**：RSG / 系统1-2 / 符号-神经融合 / 启发式自演化 / 负样本 / 元知识 / 元认知 / EDA / Data Fabric / 持续+运行时验证 / 边缘联邦。

---

## 四、建议落地优先级（由用户拍板，方向用户定）

按"单人 0 预算 30 天、严格不虚构"约束，从下往上补最该补的：

1. **【必补·核心卖点】系统2→RSG**：把单路径 A\* 升为可记录分支/合并/回溯的推理状态图。仍能确定性、可审计，不依赖 LLM——这是 v3.0"从生成到搜索"的硬证据，本地纯算法可实现。
2. **【必补·信息密度】负样本空间**：`learn` 同时存失败/临界样本，提升信息密度（AnyPhys 风，纯本地）。
3. **【可选·质量提升】启发式自演化**：搜索后用真实代价修正 `heuristic`（本地、数学简单）。
4. **【概念·需决策】元认知层 / EDA / 边缘联邦**：投入大，是否值得需用户定。

> 下一步等你定：做 1 / 2 / 3 中哪一个（或全部），我直接落代码，**不推 NPM、不推仓库**，最后一次性推。
