# 灵脑 LingNao（世界通用大脑）架构白皮书（WorldBrain Architecture）

> 本文档把灵脑作为**一个本地确定性、可审计的推理大模型**来定义，而非普通工具脚本。
> 凡推理必给依据，凡不可判定必标 𝕌（不可判定区）。不幻觉、不堆砌名词。

---

## 0. 一句话

**灵脑 = 可审计推理大模型**：内核把「世界图 → A\* 可审计推理 → 物理载体执行 → 学习闭环」封装为单一确定性引擎，对外以 **MCP stdio / 网页演示** 两种接口暴露。

它不是概率生成式 LLM；推理与审计全在本地内核，不经大模型，因此可审计、可复现、不幻觉。

---

## 1. 形式化定义（七元组）

$$
\mathbb{B} = (\mathbb{W},\ K,\ \Phi,\ \Psi,\ \Theta,\ \Lambda,\ \Xi)
$$

| 符号 | 含义 | 实装状态 |
|---|---|---|
| $\mathbb{W}$ 世界图 | $(\mathbb{V},\mathbb{E},\mathbb{w},\mathbb{P},\mathbb{C},\mathbb{U})$：节点 / 边 / 权重 / 命题 / 约束 / 不可判定区 | ✅ |
| $K$ 知识库 | 经验表 + 状态表 + 转移表 + 置信度 + 冲突检测 | ✅ |
| $\Phi$ 感知层 | NL→结构化状态（免费 LLM 或手动降级） | ✅（含 Banach 信念收敛） |
| $\Psi$ 学习层 | 反馈更新转移置信度 | ✅（含 PAC 样本界） |
| $\Theta$ 推理层 | A\* 最优路径 + 硬约束剪枝 + 软约束代价加成 | ✅ |
| $\Lambda$ 因果层 | do 演算因果发现（PC-lite + 后门调整） | ✅ lite |
| $\Xi$ 审计层 | 五段报告（概要/详细/证据/约束/附录）+ $\mathbb{U}$ 标记 | ✅ |

---

## 2. 八层（对齐产品文档）

1. **感知 $\Phi$** — 把自然语言/传感器流转成结构化世界状态
2. **学习 $\Psi$** — 执行回报更新知识库置信度
3. **知识库 $K$** — 经验、状态、转移、置信度、冲突检测
4. **推理 $\Theta$** — A\* 在约束图上的最优决策
5. **因果 $\Lambda$** — do 演算因果发现（远期）
6. **演化** — 世界图随载体经验在线更新
7. **审计 $\Xi$** — 每步依据链 + 五段报告 + $\mathbb{U}$
8. **统一** — 把以上编排为对外一致接口（MCP / Web）

---

## 3. 实装映射（诚实表）

| 模块 | 状态 | 说明 |
|---|---|---|
| $\Theta$ 推理（A\*） | ✅ | 有限图 + 可采纳欧氏启发式 → 完备且最优；系统1快答 + 系统2+RSG 推理状态图 |
| $\Xi$ 审计 | ✅ | 七段报告（含形式化证明证书+可复现）+ $\mathbb{U}$ + 不确定性量化 |
| $K$ 知识库 | ✅ | 正·负·边界样本 + 置信度 + 冲突检测 + LSH 近似检索 + FP-Growth 规则蒸馏 + 认知图谱 |
| $\mathbb{U}$ 不可判定 | ✅ | 目标缺失 / 硬约束封锁 → 诚实标 $\mathbb{U}$ |
| 物理载体接入 | ✅ | 电量/密度 → 硬软约束；<20% 禁离充电座 |
| $\Phi$ 感知（免费LLM + Banach） | ✅ | NL→JSON + 贝叶斯滤波迭代 + Banach 压缩映射收敛检测 |
| $\Psi$ PAC 界 | ✅ lite | 样本复杂度下界 m≥(d_VC·ln(1/ε)+ln(1/δ))/ε²（保证界） |
| $\Lambda$ do 演算因果 | ✅ lite | PC-lite 因果发现 + 后门准则 do 查询（离散经验库近似） |
| 符号验证（Z3-lite） | ✅ lite | 手写约束求解器 + 霍尔机器验证（逐边不变量证明） |
| D-MCTS 分支探索 | ✅ | 并行分支 + 回溯，返回多候选最优路径 |
| 第五层元认知 | ✅ | 熵 H(K)/一致性 C(K)/缺口/仲裁/探索-利用决策 |
| EDA 事件总线 | ✅ | 发布/订阅，模块解耦 |
| Data Fabric + PrSTL + 持续验证 | ✅ | 知识库版本化 + 运行时安全停车 + 仓库级验证管道 |
| 世界模型 / 反事实 | ✅（lite） | SEM 线性结构方程（手写最小二乘）+ Pearl 反事实三步法；确定性、可审计；文档仅给 VAE/ADM-v2 名词，本实装为诚实 lite 等价（非 VAE） |
| LSH 向量检索 | ✅ lite | SimHash 投影近似（非 Milvus 工业级） |

> 全部模块均可运行、确定性、可审计。标注 **lite** = 手写轻量等价实现（非工业级外部求解器 Z3/Coq/Milvus/PC-FCI），均不虚构、均经 Node 校验（43/43）。

---

## 4. 核心定理（可证）

**定理**：在有限世界图 $\mathbb{W}$ + 可采纳欧氏启发式 $h(n)=\lVert \text{coord}(n)-\text{coord}(\text{goal})\rVert$ 下，A\* **完备且最优**：

- 要么返回从起点到目标的最优路径（成本最小、满足全部硬约束、软约束代价已计入）；
- 要么诚实标 $\mathbb{U}$（目标不在图 / 硬约束封锁无解）。

---

## 5. 诚实边界

显式接受 **哥德尔不完备** 与 **休谟归纳问题**：

- 经验置信度是「用出来的熟练度」，不是真理保证；
- 远期算法除世界模型/反事实外均已确定性实装（lite 替代）：Banach 信念收敛 / PAC 界 / do 演算·PC因果 / 霍尔机器验证 / 符号Z3-lite / LSH / D-MCTS / 元认知 / EDA / PrSTL。世界模型/反事实已 lite 实装（SEM + 反事实三步法，确定性可审计）。
- 免费 LLM 仅用于感知层 NL→JSON，可手动降级，不影响内核确定性。

---

## 6. 不幻觉设计：神经符号边界与置信分层

大模型产生幻觉，根因是**概率生成**——它没有真值约束，只是从训练分布"猜"下一个 token。灵脑的架构选择是**不做生成式 LLM**，而用**神经符号**结构把 LLM 锁在感知/解释两个前端，推理与审计全在本地确定性内核。每个对外结果都带 `grounding` 字段，显式声明其可信档位：

| 档位 `tier` | 来源 | `mayHallucinate` | 可否作依据 |
|---|---|---|---|
| `UNVERIFIED_LLM`（PERCEPTION） | 免费 LLM 理解人话 | `true` | 否；标注后绝不进入证明链 |
| `DETERMINISTIC`（KERNEL） | A\* / 知识库计算 | `false` | 是；可复现 |
| `AUDITED`（PROOF） | 七段审计 + 霍尔证明 | `false` | 是；可机器验证 |

落实点（代码）：
- `setWorld` 经 `validateWorld` 校验（非空 nodes/edges、资源上限 5000/50000、边权/概率夹紧），畸形输入不进确定性内核；
- `perceiveLLM` 把 `confidence` 夹紧 `[0,1]`、`battery` 夹紧 `[0,100]`，并打 `percept._grounding=PERCEPTION` 标签；
- `askBrain` 返回 `grounding: groundingMeta()` 与 `disclaimer`，明确"感知可能幻觉、决策依据来自内核"；
- `explainWithLLM` 解释文本带 `grounding=PERCEPTION` + "可能幻觉"声明；
- `generateAudit` 带 `noHallucination:true` 与三档 `grounding`；
- `reason` / `causalEffect` 结果标 `KERNEL`（确定性、可审计）。

该保证由 `node lingnao-mcp.js --selftest` 的 `grounding` 项持续验证（43/43）。
