# 世界大脑 架构白皮书（WorldBrain Architecture）

> 本文档把世界大脑作为**一个本地确定性、可审计的推理大模型**来定义，而非普通工具脚本。
> 凡推理必给依据，凡不可判定必标 𝕌（不可判定区）。不幻觉、不堆砌名词。

---

## 0. 一句话

**世界大脑 = 可审计推理大模型**：内核把「世界图 → A\* 可审计推理 → 物理载体执行 → 学习闭环」封装为单一确定性引擎，对外以 **MCP stdio / 网页演示** 两种接口暴露。

它不是概率生成式 LLM；推理与审计全在本地内核，不经大模型，因此可审计、可复现、不幻觉。

---

## 1. 形式化定义（七元组）

$$
\mathbb{B} = (\mathbb{W},\ K,\ \Phi,\ \Psi,\ \Theta,\ \Lambda,\ \Xi)
$$

| 符号 | 含义 | 实装状态 |
|---|---|---|
| $\mathbb{W}$ 世界图 | $(\mathbb{V},\mathbb{E},\mathbb{w},\mathbb{P},\mathbb{C},\mathbb{U})$：节点 / 边 / 权重 / 命题 / 约束 / 不可判定区 | ✅ |
| $K$ 知识库 | 经验表 + 状态表 + 转移表 + 置信度 + 冲突检测 | ✅ |
| $\Phi$ 感知层 | NL→结构化状态（免费 LLM 或手动降级） | ✅（基础）/ Banach 收敛 ⏳ |
| $\Psi$ 学习层 | 反馈更新转移置信度 | ✅（单步）/ PAC 界 ⏳ |
| $\Theta$ 推理层 | A\* 最优路径 + 硬约束剪枝 + 软约束代价加成 | ✅ |
| $\Lambda$ 因果层 | do 演算因果发现 | ⏳ TODO |
| $\Xi$ 审计层 | 五段报告（概要/详细/证据/约束/附录）+ $\mathbb{U}$ 标记 | ✅ |

---

## 2. 八层（对齐产品文档）

1. **感知 $\Phi$** — 把自然语言/传感器流转成结构化世界状态
2. **学习 $\Psi$** — 执行回报更新知识库置信度
3. **知识库 $K$** — 经验、状态、转移、置信度、冲突检测
4. **推理 $\Theta$** — A\* 在约束图上的最优决策
5. **因果 $\Lambda$** — do 演算因果发现（远期）
6. **演化** — 世界图随载体经验在线更新
7. **审计 $\Xi$** — 每步依据链 + 五段报告 + $\mathbb{U}$
8. **统一** — 把以上编排为对外一致接口（MCP / Web）

---

## 3. 实装映射（诚实表）

| 模块 | 状态 | 说明 |
|---|---|---|
| $\Theta$ 推理（A\*） | ✅ | 有限图 + 可采纳欧氏启发式 → 完备且最优 |
| $\Xi$ 审计 | ✅ | 每步依据链 + 五段报告 + $\mathbb{U}$ |
| $K$ 知识库 | ✅ | 经验/状态/转移 + 置信度 + 冲突检测 |
| $\mathbb{U}$ 不可判定 | ✅ | 目标缺失 / 硬约束封锁 → 诚实标 $\mathbb{U}$ |
| 物理载体接入 | ✅ | 电量/密度 → 硬软约束；<20% 禁离充电座 |
| $\Phi$ Banach 收敛 | ⏳ TODO | 文档仅给名词，无算法定义 |
| $\Psi$ PAC 界 | ⏳ TODO | 同上 |
| $\Lambda$ do 演算 | ⏳ TODO | 同上 |
| 世界模型 / 反事实 | ⏳ TODO | 同上 |
| 霍尔形式化验证 | ⏳ TODO | 同上 |
| LSH 向量检索 | ⏳ TODO | 当前为数组精确匹配 |

> TODO 桩在代码内显式标注，未用名词堆砌假装实现。

---

## 4. 核心定理（可证）

**定理**：在有限世界图 $\mathbb{W}$ + 可采纳欧氏启发式 $h(n)=\lVert \text{coord}(n)-\text{coord}(\text{goal})\rVert$ 下，A\* **完备且最优**：

- 要么返回从起点到目标的最优路径（成本最小、满足全部硬约束、软约束代价已计入）；
- 要么诚实标 $\mathbb{U}$（目标不在图 / 硬约束封锁无解）。

---

## 5. 诚实边界

显式接受 **哥德尔不完备** 与 **休谟归纳问题**：

- 经验置信度是「用出来的熟练度」，不是真理保证；
- 远期算法（Banach / PAC / do 演算 / 世界模型 / 霍尔验证）以 TODO 桩存在，不虚构；
- 免费 LLM 仅用于感知层 NL→JSON，可手动降级，不影响内核确定性。
