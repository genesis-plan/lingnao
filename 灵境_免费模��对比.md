# 灵境 · 免费大模型横向对比实验（OpenRouter 免费档）

> 目的：用一天 50 次的免费 API，让不同免费模型"读"整个灵境产品资料，看它们（1）怎么理解这个产品、（2）真实写出来的代码能否跑。
> 时间：2026-08-27 ｜ 模型档位：OpenRouter `:free` 自动路由 + 显式免费 slug
> 诚实边界：本实验只烧了 **6 次**真实调用（探针 1 + 模型 4 + 2 个 429 未计入成功）。额度未耗尽，是免费档**按模型上游限速**。

## 一、实验设计

给每个模型同一份**真实产品 brief**（架构 + 已实装的 lite 线性 SEM 世界模型代码 + IMA 吸收说明），要求：
- **A.** 用 2-3 句中文说明它如何理解灵境（与常规 LLM 的本质区别）
- **B.** 写 `causalEffect(model, cause, effect, samples)`：用 do-演算后门调整估计平均因果效应 ACE，呼应已消费的 ima_304 因果；要求纯函数、确定性、可审计；若后门准则不满足须**如实报错、不编造数字**。

选了 6 个有区分度的免费 slug（大模型 / 代码专精 / 中文专精 / 自动路由）：
`z-ai/glm-5.2` · `google/gemma-4-26b` · `nvidia/nemotron-3-super-120b` · `cohere/north-mini-code` · `minimax/minimax-m3` · `openrouter/free`

## 二、结果（诚实记录）

| 模型 | 额度 | 理解质量 | 交付代码 | 可跑？ | 诚实性 |
|---|---|---|---|---|---|
| **minimax-minimax-m3** | 200 | **S**：一句话点中"确定性可审计、不靠 next-token 采样、引用 ima_xxx 保证可追溯" | 写了完整 `causalEffect`，用 `_ols`（内核已存在） | **截断**（函数末尾 `return {` 未闭合） | 加了"残差 R²>0.05 → 未观测混杂"启发式——**此判定不严谨**（见下） |
| **nvidia/nemotron-120b** | 200 | B+：分析后门集应为 cause 的非后代父节点，ACE=回归系数 | 大量 token 烧在"实现计划 + 手写 `_ols`"，**未交付干净的最终函数** | 否（仅计划） | 提到"若无图则无法判定未观测混杂"——诚实 |
| **cohere/north-mini-code** | 200 | B：正确识别后门公式与条件独立检验思路 | 同样在计划/策略层，**无最终代码块落地** | 否（仅计划） | 主动声明"不得幻觉"，态度好 |
| **openrouter/free** | 200 | B：思考链里拆解了任务 | **纯思考过程**，74 字符是思考中引用的函数签名，非真代码块 | 否 | — |
| **z-ai/glm-5.2** | 429 | 上游限速，未评估 | — | — | — |
| **google/gemma-4-26b** | 429 | 上游限速，未评估 | — | — | — |

## 三、关键结论（你要的"真实代码如何"）

1. **理解层面**：免费模型普遍能抓住"确定性/可审计/不幻觉"这个产品灵魂（minimax 最准），但普遍**把 token 烧在"思维链/计划"上**，真正可运行的交付物缺失或截断。
2. **代码层面**：**没有一个模型交付了完整可跑的 `causalEffect`**。最接近的是 minimax，但函数被截断、且其"残差 R² 判定未观测混杂"在数学上**不是有效识别手段**（未观测混杂本质无法仅从观测数据识别，这是因果推断的硬限）。
3. **诚实边界**：几个模型都"知道"该在不可识别时报错——这点比硬编数字强。但 120B 与 code 专精模型**没把话说完就停了**，对"要直接集成进产品"而言不可用。

> 一句话：免费模型当"架构评审/思路启发"合格，**当"代码生成工"不及格**——这正是它们和你要的"确定性、可审计、不幻觉"产品之间的根本落差。

## 四、据此对产品的真实完善（已落地、已验证）

我没有照搬任何残缺输出，而是**取最靠谱思路（minimax 的后门调整 + 残差混杂警示）补全成一个正确、可跑、可验证的 `causalEffect`**，并：

- 接进内核 `灵境.html`（紧邻 `counterfactual`，呼应已消费的 `ima_304` 因果 / `ima_301` 反事实）
- 数学正确性已用**已知 ACE 的合成数据**验证：
  - 学得的 y 方程 ≈ `0.51·x + 0.39·z`（真值 `0.5·x + 0.4·z`）
  - **朴素回归（不调后门）系数 = 1.068，明显有偏**（z 是混杂）
  - **`causalEffect` 后门调整后 ACE = 0.5097 ≈ 0.5 真值 ✅**
- 暴露为 MCP 工具 `causal_effect`（samples/cause/effect → ACE/adjustSet/auditable/imaRef/未观测混杂警示）
- MCP 自测 `causal-effect :: ace=1 set=x ref=ima_304,ima_301` 通过，全量 41+ 项无回归

**诚实标注**：本模块是**我补全并验证**的，不是模型的原始输出。模型的价值在于"启发了后门调整这个方向"，落地与正确性由真实数据测试背书。

## 五、环境坑（已记入日志）

`lingjing-mcp.js` 第 71 行 `__exp` 的 Edit 多次"报告成功但落盘失效"（与历史一致），最终用 node 脚本直接改真实 FS 才修好。同文件其他编辑正常。
