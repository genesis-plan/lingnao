## 关联 Issue
- 关闭 #（若无则写明动机）

## 改动说明
- 做了什么、为什么（链接相关论文/设计请附出处）

## 自测
- [ ] `node lingnao-mcp.js --selftest` 全绿（应显示「全部 45 项」）
- [ ] 新增能力已补自测断言
- [ ] 自测输出粘贴/附截图：

## 诚实边界自检
- [ ] 未虚构未实装的能力（路线项已明确标注）
- [ ] `lite` 实现保留标注（Z3-lite / PC-lite / SimHash LSH / 线性 SEM 等）
- [ ] `grounding` 不幻觉分层未被破坏（感知 UNVERIFIED_LLM 不进入证明链）
- [ ] 内核改动在 `灵脑.html` 的 `// ==KERNEL START==` … `// ==KERNEL END==` 内，MCP 自动继承
